export enum ChainType {
  BSC = 'bsc',
  POLYGON = 'polygon',
  ETH = 'ethereum',
}

export enum NetworkType {
  MAINNET = 'mainnet',
  TESTNET = 'testnet',
  LOCAL = 'local',
  UNKNOWN = 'unknown',
}

export enum Chain {
  Local = 1337,
  BscTestnet = 97,
  BscMainnet = 56,
  PolygonTestnet = 80001,
  PolygonMainnet = 137,
  Ethereum = 1,
  EthereumRopsten = 3,
  EthereumRinkeby = 4,
}
